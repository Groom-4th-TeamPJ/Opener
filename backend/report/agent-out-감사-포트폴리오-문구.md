# 포트폴리오 불리 문구 · 보고서 어투 전수 감사

- 대상: `/Users/mskim/Desktop/PJ/Opener/backend/docs/PORTFOLIO.html` (1,197줄, 전체 정독)
- 성격: 읽기 전용 조사. 코드·문서 수정 없음
- 감사일: 2026-07-30

## 감사 결과표

| 줄 | 원문 인용 | 유형 | 판단 | 제안 대체문구 또는 근거 |
|---|---|---|---|---|
| 169 | "결론의 사정거리를 스레드 종속성까지로 그어 뒀습니다" | A2 + B | 중립화 | 앞의 통제 설계(스레드 20개 고정 → 인과 확정)는 강점이므로 남기고, "실제 LLM 스트리밍이 아니라 대리(proxy) 부하였고 … 그어 뒀습니다" 전체를 삭제. 대체: "같은 스레드 수에서 처리량 차이가 나야 원인이 구조에 있다고 확정되므로, 측정의 목적을 숫자가 아니라 인과 확정으로 잡았습니다" |
| 170 | "남은 한계는 … 단일 인스턴스 전제라는 점입니다 — 인스턴스를 늘리면 … 갈라집니다 … 다음 과제로 정리해 뒀습니다" | A1 + A4 + B | 중립화 | 확장 한계를 자기 입으로 못 박는 문장. 대체: "SSE 전달 경로는 인스턴스 로컬이므로, 수평 확장 시에는 인스턴스 간 Redis Pub/Sub 브로드캐스트로 전달 경로를 분리하는 설계를 함께 검토했습니다" |
| 185 | "서로 다른 지표라 두 값 사이의 감소율은 계산하지 않았고" | A2 | 중립화 | 감소율을 계산하지 않았다는 고백은 불필요. 대체: "전체 응답 완료 대기 15s → 첫 토큰 약 300ms — 개선의 실체는 완료 대기에서 도착 즉시 전달로 바꾼 구조입니다" |
| 186 | "단 실제 LLM 스트리밍이 아니라 스레드 점유를 재현한 대리 부하 기준이며" | A2 | 삭제 | 169와 같은 감가를 결과 섹션에서 한 번 더 반복해 수치(5.8배·p95 2s)의 무게를 두 번 깎는다. "확인된 것은 처리량이 스레드 수에 종속되지 않는다는 구조적 결론"만 남긴다 |
| 245 | "다만 비동기 복제 특성상 쓰기 직후 조회에 복제 지연(stale read)이 있을 수 있어" | A2 유사 | 유지권장 | REPLICA_PREFERRED를 왜 "허용 범위를 정해서" 썼는지의 근거. 지우면 읽기 분산이 트레이드오프 인식 없는 선택으로 읽힌다 |
| 247 | "다만 Cluster가 주는 것은 가용성(HA)이지 내구성(Durability)이 아니라는 것" | A2 유사 | 유지권장 | Redis를 버퍼로만 쓰고 영속 경로를 따로 둔 설계 전체의 전제. 이 문장이 빠지면 RabbitMQ→PostgreSQL 경로의 존재 이유가 사라진다 |
| 252 | "다만 이 7초는 인프라 회복 시간으로, 그 구간에 유입된 쓰기는 실패로 반환되므로" | A2 | 유지권장 | 7초의 의미를 한정하는 문장. 지우면 "7초면 무손실 복구"로 읽혀 꼬리질문에서 바로 무너진다 |
| 321 | "다만 잘못된 전이를 차단하는 것과 어긋난 상태를 회수하는 것은 다르다 … 세션이 고착돼 … 경로를 발견했습니다" | A5 | 유지권장 | 자기가 발견하고 자기가 고친 케이스이며, State Machine을 "선언만 하고 끝내지 않았다"는 증거. 이 사례 없이는 항목 3이 설정 나열로 축소된다 |
| 322 | "LLM이 한 번 실패하면 SSE 연결은 살아있는데 재전송만 영구히 막히는 구조였습니다" | A5 | 유지권장 | 위와 같은 이유. 전이 2개를 추가한 판단("일시 실패는 세션을 끝낼 사유가 아니다")의 전제 |
| 330 | "대화가 영구히 막히던 경로에 상태 회수·재시도 전이를 추가해" | A5 | 중립화 | 결과 섹션에서 결함을 다시 호명한다. 대체: "빈 응답·LLM 일시 실패 시 상태를 회수하고 재전송할 수 있는 복구 경로 확보" |
| 392 | "품질 전반을 끌어올린 조치가 아니라 … 응급 처치라는 점을 구분해 뒀습니다" | A3 + B | 중립화 | 자기 조치를 "응급 처치"로 격하. 대체: "임계값 조정은 검색이 0건으로 떨어지던 상태를 되돌리는 조치이고, 근본 해결은 임베딩 모델의 언어 정렬 문제로 따로 구분했습니다" |
| 393 | "안전한 방향으로 기울여 뒀습니다" | B | 어투교정 | "…무관한 문서를 주입하는 쪽보다 안전한 방향을 택했습니다" |
| 394 | "남은 해결 방향을 세 가지로 정리해 뒀습니다 … 임계값 조정은 이 중 어느 것도 대신하지 못합니다" | A3 + A4 + B | 중립화 | 마지막 문장이 현재 구현을 스스로 부정한다. 대체: "근본 해결책으로 다국어 임베딩 모델 교체, BM25 하이브리드 검색, Reranker 재정렬 세 가지를 비교했고, 현 단계에서는 비용 대비 효과를 근거로 임계값 재조정을 선택했습니다" |
| 396 | "오프너 분석 경로는 이 짝이 없어 플래그를 끄면 컨텍스트 기동 자체가 실패했습니다" / "주석은 '부팅 실패 방지'라고 적혀 있었는데 부팅 실패를 유발하고 있었습니다" | A5 | 중립화 | 교훈은 살리되 자백 프레이밍을 줄인다. 대체: "조건부 빈은 '빈을 만들지 않는 것'만으로 안전해지지 않고, 그 타입을 주입받는 쪽까지 함께 봐야 방어가 성립합니다. 같은 인터페이스에 사용·미사용 구현체를 짝으로 두어 플래그 양방향에서 빈이 항상 하나 존재하도록 정리했습니다" |
| 397 | "수정한 뒤에도 기동이 여전히 실패해서 원인을 다시 추적했습니다" | A5 | 중립화 | "고쳤는데 또 실패"라는 서술은 시행착오만 남긴다. 대체: "같은 벡터 저장소 빈을 주입받는 클래스가 다섯 개였고, 조건이 걸리지 않은 하나는 ObjectProvider로 선택적 의존으로 바꿨습니다 — 짝을 늘리면 그 소비자를 또 확인해야 해 연쇄가 끊기지 않기 때문입니다" |
| 398 | "DB에 영구 저장되는 대화 요약이 최근 20턴 기준으로 잘리는 회귀를 발견해" | A5 | 유지권장 | 히스토리 조회 계약을 둘(컨텍스트용/영속화용)로 나눈 유일한 근거. 지우면 "왜 인터페이스가 두 벌인가"에 답할 수 없다 |
| 405 | "플래그로 RAG를 끄는 경로도 기동 실패하던 것을 고쳐, 이제 양방향 모두 정상 기동합니다(위 참조)" | A5 | 중립화 | 결과 섹션에서 결함을 재호명. 대체: "RAG On/Off 양방향을 실제로 기동해 두 경로 모두 정상 동작함을 검증했습니다" |
| 408 (제목) | "무력화돼 있던 인증·세션 방어 장치 3건을 정적 감사로 발견해 복구" | A5 + B | 중립화 | 섹션 제목이라 가중치가 가장 크다. "무력화돼 있던"은 결함 자백, "감사"는 보고서 용어. 대체: "로그아웃 토큰 무효화·SSE 세션 소유권 검증을 코드 수준 전수 대조로 점검해 보강" |
| 461 | "구현했다고 믿고 있던 장치가 실제로 작동하는지 코드 수준에서 대조한 적이 없었습니다" | A5 | 중립화 | 대체: "기능이 동작하는 것과 방어 장치가 동작하는 것은 다르다고 보고, 인증·세션 방어선을 Redis 키 단위로 전수 대조했습니다" |
| 462 | "blacklist:access:{jti}에 쓰기만 하고 읽는 코드가 어디에도 없어" | A5 | 중립화 | 지시서에 명시된 자백 프레이밍. 대체: "로그아웃 시 블랙리스트 등록만 있고 인증 필터에 조회·거부 단계가 없어, access token 무효화가 요청 경로에서 완결되지 않는 구조였습니다" |
| 463 | "access token을 다시 발급받는 경로가 열려 있었습니다" | A5 | 중립화 | 대체: "서버 보관본 refreshToken:{userId}가 로그아웃 대상에 포함되지 않아, 무효화 범위가 access token 한쪽에만 걸쳐 있었습니다" |
| 464 | "TTL이 남은 다른 사용자의 세션에 그대로 올라탈 수 있는 비대칭이 있었습니다" | A5 | 중립화 | 침해 시나리오를 직서술한다. 대체: "SSE 연결 생성의 두 분기 중 신규 연결 경로에만 소유자 검증이 걸리지 않는 비대칭이 있었습니다" |
| 469 | "방어 장치는 '쓰는 코드가 있다'가 아니라 '읽고 거부하는 코드가 있다'로만 검증된다고 보고" | — | 유지권장 | 감사 절차의 판단 기준 자체이고 어투도 보고서체가 아니다. 이 문장이 항목 5를 "우연히 버그를 봤다"가 아니라 "기준을 세워 훑었다"로 만든다 |
| 472 | "다만 시스템 전체의 소유자 검증이 한 곳으로 모인 것은 아닙니다 … 두 곳이 같은 대조 로직을 각각 독립적으로 구현 … 지금이라면 한쪽이 다른 쪽에 위임하도록 합치겠습니다" | A3 + A4 | 삭제 | ①현재 구현 열등 + ②미완 후속을 한 문장에 담아 감점이 겹친다. ③**사실 확인 필요**: 최근 커밋 `281ded3 refactor : 세션 소유자 검증 중복 구현 통합 및 테스트 추가`로 이 중복은 이미 해소된 것으로 보이므로 문서가 낡았을 가능성이 높다. 앞 절("호출부에 조건문을 더하는 대신 세션 초기화 안쪽으로 검증을 넣어 … 검증이 새지 않습니다")까지만 남긴다 |
| 473 | "문서에 적힌 SSE 30분 타임아웃, dev 1분 계정 잠금, prod 로그 레벨이 전부 미적용 상태였습니다" | A5 | 유지권장 | "Spring Boot는 모르는 프로퍼티를 조용히 무시한다"는 함정을 실측 사례로 뒷받침한다. 지우면 474~476의 런타임 바인딩 검증 절차가 왜 필요한지가 설명되지 않는다 |
| 483 | "런타임 바인딩을 확인하는 절차로 정립 — 세 건 모두 실서비스 침해가 아니라 정적 분석으로 찾은 결함이며" | A5 + B | 삭제 | "정립"은 보고서 어투, "실서비스 침해가 아니라"는 묻지도 않은 방어이며 '침해'라는 단어를 면접관 머릿속에 심는다. 대체: "설정 6곳의 실효값을 런타임에서 확인하는 절차를 만들고, 세 건 모두 실행 검증까지 마쳤습니다" |
| 645 | "임계 구역이 Redis 밖으로 나가는가라는 단일 기준이 두 도메인의 선택을 모두 설명합니다" | — | 유지권장 | 쿠폰은 락을 쓰고 재고는 안 쓰는 이유를 하나의 기준으로 통합한 문장. 이 프로젝트에서 설계 사고를 가장 잘 보여주는 대목 |
| 648 | "지금 다시 만든다면 락 범위를 폴백 경로로 좁히겠습니다 — … 현재는 전 구간에 락이 걸려 … 처리량 상한이 내려갑니다" | A3 | 중립화 | 전형적인 "현재 구현이 열등" 회고. 대체: "락 범위를 폴백 경로로만 좁히는 것이 이론적 최적이지만, 락 보유 구간이 Lua 실행 시간 수준이라 실측 p99 85ms에서 병목이 아니었고, 경계를 단순하게 유지하는 편이 안전하다고 판단했습니다" |
| 649 | "확인한 한계와 후속 조치는 두 가지입니다 … 경합은 별도 에러코드로 분리해야 합니다" | A4 + B | 중립화 | 서두는 보고서 어투, ①은 미완처럼 읽힌다(②는 이미 "후속 도입했습니다"). 대체: "락 획득 실패가 품절과 같은 응답으로 나가지 않도록 경합 전용 에러코드를 분리했고, 단일 핫 쿠폰 대비로 비동기 발급 경로와 ZSet 기반 추정 대기 순번 표시를 도입했습니다" (①이 미구현이라면 아예 삭제) |
| 740 | "보상(취소) 경로에도 같은 원칙을 적용하도록 설계했습니다" | A4 | 중립화 | "설계했습니다"가 구현 미완으로 읽힌다. 구현했다면 "적용했습니다", 설계 단계에서 멈췄다면 뒤의 멱등키 3원칙 설명만 남기고 앞 절 삭제 |
| 747 | "장애 주입(DB 실패 1,000건) 카오스 테스트에서 자동 복구율 98.7% (987건)" | A6 | 유지권장 | 100% 미만이지만 98.7%는 그 자체로 강한 수치이고, 카오스 테스트 설계를 했다는 사실이 더 크게 읽힌다 |
| 800 | "재시도 횟수를 Micrometer로 계측 — 평균 0.35회/건(최대 3회)" | — | 유지권장 | "재시도 3회가 타당한가"를 데이터로 답한 부분. 이 수치가 801의 미계측 고백을 상쇄한다 |
| 801 | "확인한 한계 두 가지를 개선 항목으로 정리했습니다 … 회수 절차가 필요합니다 … 실패 사유를 코드별로 분리 계측하지 않아 … 다음 단계입니다" | A2 + A4 + B | 중립화 | 보고서 서두 + 미완 2건 + 미계측 자백이 한 항목에 몰려 있다. 대체: "선점 키에 TTL을 두고 DB 재고와 주기적으로 대조하는 회수 절차, 실패 경로별 카운터 분리를 다음 개선 항목으로 잡았습니다" — 미계측 언급은 808과 중복이므로 한 곳에만 남긴다 |
| 806 | "동시 차감 1만 건을 시도해 9,685건 차감" | A6 | 중립화 | 성공 건수가 먼저 읽혀 "3%가 실패했다"로 보인다. 정합성 결과를 전면에: "재고 10,000개에 1만 건 동시 차감 부하 — 종료 후 DB와 Redis 재고가 315로 정확히 일치, 초과판매·저장소 간 드리프트 0건" |
| 807 | "동시 10건 고경합 부하에서 재고 차감 p95 352.8ms" | A2 | 중립화 | "동시 10건"이 작게 읽혀 352.8ms가 느려 보인다. 부하 조건을 VU/총 요청 수 기준으로 다시 쓰거나, 이 항목에서는 지표를 빼고 "PG 호출 전 조기 차단" 효과만 서술 |
| 808 | "재시도 소진으로 추정되며 … 실패 사유를 분리 계측하지 않았으므로 원인은 추정입니다" | A2 + A6 | 삭제 | 항목의 마지막 문장에서 자기 수치의 신뢰도를 스스로 무너뜨린다. "초과판매 0건과 315건 실패는 같은 선택의 양면이고, 정해진 재시도 안에 성공하지 못한 요청은 실패로 되돌려 정확성을 지켰습니다"까지만 |
| 895 | "12s는 산술 추정 … 개선 후만 측정한 이유는 폴링 구현을 제거해 동일 조건 A/B가 불가능했기 때문이며" | A2 | 중립화 | 출처 구분(추정/실측)은 정직성의 강점이므로 남기고, "A/B가 불가능했다"는 해명만 뺀다. 대체: "12s는 설정에서 도출한 최악값(폴링 주기 10s + 평균 스캔 2s), 1.1s는 이벤트 발행~비활성 전환 시각 차 실측값입니다" |
| 1128 | "불필요한 컨슈머 오프셋 관리 복잡도를 추가하지 않는 방향으로 설계 범위를 제한했습니다" | A3 유사 | 유지권장 | 오버엔지니어링을 피한 의도적 판단으로 읽힌다. 감점 요소가 아니라 요구사항 대비 적정 설계의 근거 |

## 유형별 집계

| 유형 | 건수 | 해당 줄 |
|---|---|---|
| A1 확장 한계 자기 명시 | 1 | 170 |
| A2 측정 신뢰도 감가 | 8 | 169, 186, 245*, 252*, 801, 807, 808, 895 |
| A3 현재 구현 열등 회고 | 4 | 392, 394, 648, 1128* |
| A4 미완처럼 보이는 후속 | 5 | 170, 394, 472, 649, 740, 801 |
| A5 결함 자백 프레이밍 | 11 | 321*, 322*, 330, 396, 397, 398*, 405, 408, 461, 462, 463, 464, 473*, 483 |
| A6 100% 미만 수치 노출 | 3 | 747*, 806, 808 |
| B 보고서 어투 | 8 | 169, 170, 392, 393, 394, 408, 483, 649, 801 |

(*는 `유지권장` 판정)

- 총 지적: 34행 / 그중 `유지권장` 11행, `중립화` 17행, `삭제` 5행, `어투교정` 1행
- 감점 밀도가 가장 높은 구간: **항목 4(RAG) 392~397·405**, **항목 5(보안) 408·461~464·472·483**, **이커머스 항목 3(재고) 801·806~808**

## 별도 확인이 필요한 사실 관계

1. **472행이 낡았을 가능성** — 최근 커밋 `281ded3 refactor : 세션 소유자 검증 중복 구현 통합 및 테스트 추가`가 이 문단이 "남아 있다"고 서술한 중복을 이미 해소한 것으로 보인다. 문서를 고치기 전에 실제 코드 상태를 확인해야 한다.
2. **649행 ①(경합 전용 에러코드)과 740행(보상 경로 멱등키)** — 구현 완료인지 계획 단계인지에 따라 제안 문구가 갈린다. 구현됐다면 "…했습니다"로 승격, 미구현이면 문장 자체를 빼는 편이 낫다.
3. **807행 "동시 10건"** — 실제 k6 시나리오의 VU/요청 수를 확인해 정확한 부하 조건으로 다시 표기할 필요가 있다.
