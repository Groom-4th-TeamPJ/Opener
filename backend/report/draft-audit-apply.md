# 포트폴리오 감사 적용 초안 (eco-1 제외)

- 대상: `/Users/mskim/Desktop/PJ/Opener/backend/docs/PORTFOLIO.html` (현재 1,392줄)
- 지시서: `backend/report/agent-out-감사-포트폴리오-문구.md`
- 작성일: 2026-07-30
- 성격: **초안만 작성. PORTFOLIO.html 은 편집하지 않았음**

감사표의 줄번호는 이미 밀렸으므로 **원문 문자열로 재탐색**해 위치를 확정했습니다.
아래 "현재 원문"은 모두 현재 파일에서 그대로 복사한 것이고, 각 항목은 `<li>` 한 줄 통째 치환입니다
(선행 공백 2칸 포함, 문자열 치환 시 그대로 사용 가능).

---

## 1. 적용 대조표 (11건)

### ① 감사표 185행 → 현재 184행 · 중립화

**현재 원문**
```html
  <li>사용자가 첫 글자를 보기까지 <span class="metric">전체 응답 완료 대기 15s → 첫 토큰 약 300ms</span> — 서로 다른 지표라 두 값 사이의 감소율은 계산하지 않았고, 개선의 실체는 <b>완료 대기에서 도착 즉시 전달로 바꾼 구조</b>입니다</li>
```

**대체문**
```html
  <li>사용자가 첫 글자를 보기까지 <span class="metric">전체 응답 완료 대기 15s → 첫 토큰 약 300ms</span> — 응답 완료 시점과 첫 토큰 도착 시점을 각각 재는 서로 다른 지표이고, 개선의 실체는 <b>완료 대기에서 도착 즉시 전달로 바꾼 구조</b>입니다</li>
```

**사유** — "감소율은 계산하지 않았다"는 자기 수치를 스스로 감가하는 문장입니다. 두 지표가 다르다는 사실(정직성의 근거)은 그대로 유지하고, 계산을 안 했다는 고백만 뺐습니다. 수치 15s·300ms 는 보존했습니다.

---

### ② 감사표 186행 → 현재 185행 · 삭제(대리 부하 감가 절)

**현재 원문**
```html
  <li>스레드 20개 고정 통제 실험에서 처리량 <span class="metric">9.5 → 55 req/s(약 5.8배)</span>, p95 <span class="metric">20s → 2s</span>를 k6로 확인 — 단 실제 LLM 스트리밍이 아니라 스레드 점유를 재현한 대리 부하 기준이며, 확인된 것은 처리량이 스레드 수에 종속되지 않는다는 구조적 결론입니다</li>
```

**대체문**
```html
  <li>스레드 20개 고정 통제 실험에서 처리량 <span class="metric">9.5 → 55 req/s(약 5.8배)</span>, p95 <span class="metric">20s → 2s</span>를 k6로 확인 — 같은 스레드 수에서 벌어진 차이이므로, 확인된 것은 <b>처리량이 스레드 수에 종속되지 않는다</b>는 구조적 결론입니다</li>
```

**사유** — 대리(proxy) 부하라는 단서는 이미 **169행 해결 과정에 온전히 남아 있습니다**("다만 부하는 실제 LLM 스트리밍이 아니라 … 결론 내렸습니다"). 결과 섹션에서 한 번 더 반복하면 5.8배·p95 2s 의 무게를 두 번 깎게 되므로, 여기서는 통제 조건(같은 스레드 수)과 결론만 남겼습니다. 단서 자체가 문서에서 사라지지는 않습니다.

---

### ③ 감사표 330행 → 현재 329행 · 중립화

**현재 원문**
```html
  <li>빈 응답·LLM 일시 실패로 세션이 <span class="mono">PROCESSING</span>에 고착돼 대화가 영구히 막히던 경로에 상태 회수·재시도 전이를 추가해 복구 경로 확보</li>
```

**대체문**
```html
  <li>빈 응답·LLM 일시 실패로 <span class="mono">PROCESSING</span>에 남은 상태를 회수하고 같은 세션에서 재전송할 수 있도록 전이를 보강해 복구 경로 확보</li>
```

**사유** — 결과 섹션에서 "대화가 영구히 막히던" 결함을 다시 호명합니다. 320·321행(유지권장)에 원인·발견 과정이 이미 상세히 서술돼 있으므로, 결과에서는 확보된 능력(상태 회수 + 같은 세션 재전송)으로 서술합니다. `PROCESSING` 식별자는 보존했습니다.

---

### ④ 감사표 392행 → 현재 391행 · 중립화

**현재 원문**
```html
  <li>"임계값이 높을수록 정확하다"는 통념과 달리 다국어 임베딩 조합에서는 유사도 분포 자체가 낮게 깔린다는 것을 데이터로 확인하고, 임계값을 0.7에서 0.2로 낮춰 재현율을 회복했습니다 — <b>정밀도를 내주고 재현율을 산 교환</b>이며, 이것이 품질 전반을 끌어올린 조치가 아니라 검색이 0건으로 떨어지던 상태를 되돌린 응급 처치라는 점을 구분해 뒀습니다</li>
```

**대체문**
```html
  <li>"임계값이 높을수록 정확하다"는 통념과 달리 다국어 임베딩 조합에서는 유사도 분포 자체가 낮게 깔린다는 것을 데이터로 확인하고, 임계값을 0.7에서 0.2로 낮춰 재현율을 회복했습니다 — <b>정밀도를 내주고 재현율을 산 교환</b>이며, 임계값 조정은 검색이 0건으로 떨어지던 상태를 되돌리는 조치이고 근본 해결은 임베딩 모델의 언어 정렬 문제라고 층을 나눠 잡았습니다</li>
```

**사유** — "응급 처치"는 자기 조치를 스스로 격하하는 단어입니다. 조치의 사정거리를 구분한다는 **판단 자체는 강점**이므로 "조치 층 / 근본 원인 층을 나눴다"는 설계 서술로 바꿨습니다. 0.7 → 0.2 수치는 보존했습니다.

---

### ⑤ 감사표 393행 → 현재 392행 · 어투교정

**현재 원문**
```html
  <li>임계값을 낮추면 무관 문서가 섞여 들어올 위험이 커지므로, Top-K 5로 주입 컨텍스트 수를 제한하고 대상 코퍼스를 강의자료·기출로 좁혔습니다. 그래도 검색 결과가 비면 컨텍스트를 억지로 채우지 않고 <b>RAG 없는 프롬프트로 폴백</b>하도록 해, 무관한 문서를 주입하는 쪽보다 안전한 방향으로 기울여 뒀습니다</li>
```

**대체문**
```html
  <li>임계값을 낮추면 무관 문서가 섞여 들어올 위험이 커지므로, Top-K 5로 주입 컨텍스트 수를 제한하고 대상 코퍼스를 강의자료·기출로 좁혔습니다. 그래도 검색 결과가 비면 컨텍스트를 억지로 채우지 않고 <b>RAG 없는 프롬프트로 폴백</b>하도록 해, 무관한 문서를 주입하는 쪽보다 안전한 방향을 택했습니다</li>
```

**사유** — "기울여 뒀습니다"는 결정을 미뤄 둔 어감입니다. 같은 내용을 확정된 선택("택했습니다")으로 씁니다. 문장 끝 한 어절만 바뀝니다.

---

### ⑥ 감사표 394행 → 현재 393행 · 중립화

**현재 원문**
```html
  <li>근본 원인은 임계값이 아니라 <b>한국어 질문과 영어 중심 임베딩 모델 사이의 언어 정렬 문제</b>라고 판단했고, 남은 해결 방향을 세 가지로 정리해 뒀습니다 — ① 다국어 임베딩 모델로 유사도 분포를 정상화, ② 키워드 검색(BM25)과 벡터 검색을 결합해 절대 임계값 의존을 줄이기, ③ 넉넉히 뽑고 Reranker로 재정렬. 임계값 조정은 이 중 어느 것도 대신하지 못합니다</li>
```

**대체문**
```html
  <li>근본 원인은 임계값이 아니라 <b>한국어 질문과 영어 중심 임베딩 모델 사이의 언어 정렬 문제</b>라고 판단하고, 근본 해결책 세 가지를 비교했습니다 — ① 다국어 임베딩 모델로 유사도 분포를 정상화, ② 키워드 검색(BM25)과 벡터 검색을 결합해 절대 임계값 의존을 줄이기, ③ 넉넉히 뽑고 Reranker로 재정렬. 셋 다 임베딩 모델 교체나 검색 파이프라인 증설을 동반하므로, 현 단계에서는 비용 대비 효과를 근거로 임계값 재조정과 폴백을 먼저 택했습니다</li>
```

**사유** — 원문 마지막 문장("이 중 어느 것도 대신하지 못합니다")이 현재 구현을 스스로 부정합니다. 세 대안의 내용(BM25·Reranker·다국어 임베딩)은 **비교 검토를 했다는 근거**이므로 전부 유지하고, 결론만 "미완의 숙제"에서 "비용 대비 효과 기준의 선택"으로 바꿨습니다. "남은 해결 방향"(미완 톤) → "근본 해결책 … 비교했습니다".

---

### ⑦ 감사표 396행 → 현재 395행 · 중립화

**현재 원문**
```html
  <li>반면 <b>오프너 분석 경로는 이 짝이 없어 플래그를 끄면 컨텍스트 기동 자체가 실패</b>했습니다 — <span class="mono">RagService</span>가 활성 조건이 붙은 구현체 하나뿐이라 플래그를 끄면 빈이 사라지는데 주입받는 쪽이 필수 의존이었습니다. 조건부 빈을 넣을 때 "빈을 안 만들면 안전하다"까지만 보고 <b>주입받는 쪽을 함께 보지 않으면 방어가 정확히 반대로 작동</b>합니다 — 실제로 해당 구현체의 주석은 "부팅 실패 방지"라고 적혀 있었는데 부팅 실패를 유발하고 있었습니다. 같은 인터페이스에 두 구현체를 짝으로 두는 방식으로 수정했습니다</li>
```

**대체문**
```html
  <li>반면 <b>오프너 분석 경로에는 이 짝이 없었습니다</b> — <span class="mono">RagService</span>가 활성 조건이 붙은 구현체 하나뿐이라 플래그를 끄면 빈이 사라지는데, 주입받는 쪽은 필수 의존이었습니다. 조건부 빈은 "빈을 만들지 않는 것"만으로 안전해지지 않고 <b>그 타입을 주입받는 쪽까지 함께 봐야 방어가 성립</b>합니다. 같은 인터페이스에 사용·미사용 구현체를 짝으로 두어 플래그 양방향에서 빈이 항상 하나 존재하도록 정리했습니다</li>
```

**사유** — "컨텍스트 기동 자체가 실패했습니다" + "주석은 부팅 실패 방지라고 적혀 있었는데 부팅 실패를 유발" 두 절이 결함 자백 프레이밍의 핵심입니다. **교훈 문장(조건부 빈은 주입받는 쪽까지 봐야 한다)은 그대로 살렸고**, 자백 서술만 원인 구조 서술로 바꿨습니다. `RagService` 식별자는 보존했습니다.

> 참고: 주석 일화("부팅 실패 방지"라고 적힌 주석이 부팅 실패를 유발)는 이야기로서는 강하지만, 포트폴리오 본문보다 **면접 구두 답변으로 꺼낼 때 훨씬 유리**합니다. 문서에서는 빼고 답변 카드로 옮기는 것을 권합니다.

---

### ⑧ 감사표 397행 → 현재 396행 · 중립화

**현재 원문**
```html
  <li>수정한 뒤에도 <b>기동이 여전히 실패해서 원인을 다시 추적했습니다</b> — 같은 벡터 저장소 빈을 주입받는 클래스가 다섯 개였고 그중 하나만 조건이 빠져 있었습니다. 이쪽은 짝을 또 만드는 대신 <b>의존성 자체를 선택적으로</b>(<span class="mono">ObjectProvider</span>) 받도록 바꿨습니다 — 짝을 늘리면 그 짝의 소비자를 또 확인해야 해 연쇄가 끊기지 않고, 이 서비스는 검색 실패 시 빈 컨텍스트로 진행하도록 이미 설계돼 있어 선택적 의존이 코드의 의도와 일치합니다. <b>플래그를 양방향으로 실제 기동해 확인</b>했습니다(끈 상태·켠 상태 모두 정상 기동, 두 구현체가 동시에 뜨는 빈 충돌 없음)</li>
```

**대체문**
```html
  <li>같은 벡터 저장소 빈을 주입받는 클래스가 다섯 개라는 것도 이때 함께 확인했고, 조건이 걸리지 않은 하나는 <b>의존성 자체를 선택적으로</b>(<span class="mono">ObjectProvider</span>) 받도록 바꿨습니다 — 짝을 늘리면 그 짝의 소비자를 또 확인해야 해 연쇄가 끊기지 않고, 이 서비스는 검색 실패 시 빈 컨텍스트로 진행하도록 이미 설계돼 있어 선택적 의존이 코드의 의도와 일치합니다. <b>플래그를 양방향으로 실제 기동해 확인</b>했습니다(끈 상태·켠 상태 모두 정상 기동, 두 구현체가 동시에 뜨는 빈 충돌 없음)</li>
```

**사유** — 앞머리 "수정한 뒤에도 기동이 여전히 실패해서"만 제거했습니다. 나머지(다섯 개 클래스, `ObjectProvider` 선택 이유, 양방향 기동 검증)는 **판단 근거이자 검증 증거**라 전부 보존했습니다. 시행착오 서사 → 전수 확인 서사로 프레임만 바뀝니다.

---

### ⑨ 감사표 405행 → 현재 404행 · 중립화

**현재 원문**
```html
  <li>검색 결과가 0건일 때 RAG 컨텍스트를 비우고 일반 프롬프트로 폴백해, 문서를 못 찾은 상황이 응답 실패로 번지지 않도록 처리 — <b>플래그로 RAG를 끄는 경로</b>도 기동 실패하던 것을 고쳐, 이제 양방향 모두 정상 기동합니다(위 참조)</li>
```

**대체문**
```html
  <li>검색 결과가 0건일 때 RAG 컨텍스트를 비우고 일반 프롬프트로 폴백해, 문서를 못 찾은 상황이 응답 실패로 번지지 않도록 처리 — <b>RAG On/Off 양방향</b>을 실제로 기동해 두 경로 모두 정상 동작함을 검증했습니다(위 참조)</li>
```

**사유** — 결과 섹션에서 "기동 실패하던 것을 고쳐"로 결함을 재호명합니다. 검증 사실(양방향 실기동)만 남기면 같은 성과가 감점 없이 전달됩니다.

---

### ⑩ 감사표 740행 → 현재 932행 · 중립화 (eco-2 구간, eco-1 아님)

**현재 원문**
```html
  <li>보상(취소) 경로에도 같은 원칙을 적용하도록 설계했습니다 — 멱등키가 만족해야 하는 성질을 <b>① 동일 의도는 동일 키 ② 다른 의도는 다른 키 ③ 프로세스 재시작에도 살아남을 것</b> 세 가지로 정리하고, 보상 레코드의 식별자처럼 <b>재시도 간 불변인 값</b>을 키로 삼는 것을 기준으로 잡았습니다. 재시도 횟수처럼 시도마다 변하는 값이 키에 섞이면 첫 번째 성질이 깨져 "상대는 취소를 처리했는데 응답만 타임아웃된" 경우를 새 요청으로 오인하게 되고, 그러면 멱등키가 목적을 잃습니다</li>
```

**대체문**
```html
  <li>보상(취소) 경로에도 승인 경로와 같은 기준을 세웠습니다 — 멱등키가 만족해야 하는 성질을 <b>① 동일 의도는 동일 키 ② 다른 의도는 다른 키 ③ 프로세스 재시작에도 살아남을 것</b> 세 가지로 정리하고, 보상 레코드의 식별자처럼 <b>재시도 간 불변인 값</b>을 키로 삼는 것을 기준으로 잡았습니다. 재시도 횟수처럼 시도마다 변하는 값이 키에 섞이면 첫 번째 성질이 깨져 "상대는 취소를 처리했는데 응답만 타임아웃된" 경우를 새 요청으로 오인하게 되고, 그러면 멱등키가 목적을 잃습니다</li>
```

**사유** — "적용하도록 설계했습니다"의 "설계했습니다"가 구현 미완으로 읽힙니다. 다만 **"적용했습니다"로 승격하면 사실과 어긋납니다**(아래 경고 참조). 그래서 기준 수립이라는 사실만 명확히 하는 "같은 기준을 세웠습니다"로 바꿨습니다. 멱등키 3원칙 설명은 그대로 유지합니다.

> ⚠️ **사실 관계 경고 — 이 항목은 문구 교정만으로 끝나지 않습니다.**
> 실제 코드 `groom-shopping/backend/src/main/java/groom/backend/application/payment/PaymentCompensationService.java:96` 은
> ```java
> String idempotencyKey = compensation.getPaymentKey() + ":compensate:" + compensation.getRetryCount();
> ```
> 로, **재시도 횟수를 멱등키에 섞고 있습니다.** 바로 이 `<li>` 가 "하면 안 된다"고 서술한 그 패턴입니다.
> 90행 주석도 "멱등성 키(paymentKey + 재시도 횟수)", 95행 주석은 "재시도 횟수를 섞어 매 시도를 구분"이라고 의도를 명시합니다.
> 즉 문서의 기준과 코드가 정반대이며, 면접에서 코드를 함께 보면 즉시 드러납니다.
> **선택지는 둘입니다.** ㉠ 코드를 기준에 맞게 고치고 문구를 "적용했습니다"로 승격 ㉡ 문서에서 이 `<li>` 를 통째로 삭제.
> 어느 쪽도 제 담당 범위를 넘으므로 **변경하지 않고 보고만 합니다.** 위 대체문은 그때까지의 임시 안전판입니다.

---

### ⑪ 감사표 895행 → 현재 1090행 · 중립화

**현재 원문**
```html
  <li>처리 지연 <span class="metric">최대 12s → 1.1s (약 91% 감소)</span> — 두 값의 출처가 다릅니다. <b>12s는 산술 추정</b>(폴링 주기 10s + 스캔 2s를 더한 최악값)이고 <b>1.1s는 실측</b>(이벤트 발행~비활성 전환 시각 차 직접 측정)입니다. 개선 후만 측정한 이유는 폴링 구현을 제거해 동일 조건 A/B가 불가능했기 때문이며, 그래서 기준값은 설정에서 도출했습니다</li>
```

**대체문**
```html
  <li>처리 지연 <span class="metric">최대 12s → 1.1s (약 91% 감소)</span> — 두 값의 출처가 다릅니다. <b>12s는 폴링 설정에서 도출한 최악값</b>(폴링 주기 10s + 스캔 2s)이고, <b>1.1s는 실측값</b>(이벤트 발행~비활성 전환 시각 차 직접 측정)입니다</li>
```

**사유** — 출처 구분(추정/실측)은 정직성의 강점이므로 **그대로 유지**하되, "A/B가 불가능했기 때문" 해명만 제거했습니다. "산술 추정" → "설정에서 도출한 최악값"으로 바꾸면 같은 사실이 근거 있는 산출값으로 읽힙니다. 12s·1.1s·91%·10s·2s 수치는 전부 보존했습니다.

---

## 2. 건너뛴 행과 사유

### 이미 처리 완료 (팀 리드 확인분 — 현재 파일에서 재확인함)

| 감사표 행 | 확인 결과 |
|---|---|
| 169 | 현재 169행이 "…라고 결론 내렸습니다"로 끝남. 교정 완료 |
| 170 | 단일 인스턴스 전제 항목이 현재 문서에 없음. 삭제 완료 |
| 408 | 현재 407행 제목이 "5. 인증·세션 방어선을 단일 통과점 구조로 설계하고 런타임 검증 절차를 확립". 재작성 완료 |
| 461·462·463·464 | 해당 자백 문구가 현재 문서에 없음. 재작성 완료 |
| 472 | 소유자 검증 중복 서술 없음. 교정 완료 |
| 483 | "실서비스 침해가 아니라" 없음. 재작성 완료 |
| 801·806·807·808 | eco-3 전면 재작성 완료 |

### 담당 외

| 감사표 행 | 사유 |
|---|---|
| 645·648·649 | `id="eco-1"`(현재 585~880행) 구간. 다른 담당이 수정 중이라 미접촉 |

### 유지권장 11행 — 원문 그대로 둠

| 감사표 행 | 유지 근거 (감사표 판단에 동의) |
|---|---|
| 245 | 복제 지연(stale read) 인식이 `REPLICA_PREFERRED` 선택의 트레이드오프 근거 |
| 247 | "Cluster는 HA지 Durability가 아니다"가 RabbitMQ→PostgreSQL 영속 경로의 존재 이유 |
| 252 | 7초를 "인프라 회복 시간"으로 한정하지 않으면 꼬리질문에서 무손실 복구로 오독됨 |
| 321 | (현재 320행) State Machine을 선언에서 끝내지 않았다는 유일한 증거 |
| 322 | (현재 321행) 전이 2개 추가 판단의 전제 |
| 398 | (현재 397행) 히스토리 조회 계약을 둘로 나눈 유일한 근거 |
| 469 | 방어 장치 검증 기준 자체. 항목 5를 "기준을 세워 훑었다"로 만듦 |
| 473 | Spring Boot가 모르는 프로퍼티를 조용히 무시한다는 함정의 실측 사례 |
| 747 | (현재 939행) 98.7%는 그 자체로 강한 수치, 카오스 테스트 설계 사실이 더 크게 읽힘 |
| 800 | 재시도 3회의 타당성을 데이터로 답한 부분 |
| 1128 | 오버엔지니어링 회피의 의도적 판단으로 읽힘 |

**이견 없음.** 판단을 달리해야 할 항목은 없다고 봅니다.
다만 ⑩(932행)은 유지권장이 아니라 **코드-문서 불일치**라는 별개 사유로 후속 결정이 필요합니다.

---

## 3. 검증 — 원문 문자열 유일성

11개 대체 대상 `<li>` 를 각각 전체 문자열로 검색한 결과, **전부 문서 내 1회만 등장**합니다.
부분 문자열 단위로도 교차 확인했습니다(14개 조각 전부 count = 1).

| # | 현재 줄 | 검색 키 | 등장 횟수 |
|---|---|---|---|
| ① | 184 | `두 값 사이의 감소율은 계산하지 않았고` | 1 |
| ② | 185 | `스레드 점유를 재현한 대리 부하 기준이며` | 1 |
| ③ | 329 | `대화가 영구히 막히던 경로에` | 1 |
| ④ | 391 | `되돌린 응급 처치라는 점을 구분해 뒀습니다` | 1 |
| ⑤ | 392 | `안전한 방향으로 기울여 뒀습니다` | 1 |
| ⑥ | 393 | `남은 해결 방향을 세 가지로 정리해 뒀습니다` / `이 중 어느 것도 대신하지 못합니다` | 1 / 1 |
| ⑦ | 395 | `이 짝이 없어 플래그를 끄면 컨텍스트 기동 자체가 실패` / `두 구현체를 짝으로 두는 방식으로 수정했습니다` | 1 / 1 |
| ⑧ | 396 | `수정한 뒤에도 <b>기동이 여전히 실패해서 원인을 다시 추적했습니다</b>` | 1 |
| ⑨ | 404 | `도 기동 실패하던 것을 고쳐, 이제 양방향 모두 정상 기동합니다(위 참조)` | 1 |
| ⑩ | 932 | `보상(취소) 경로에도 같은 원칙을 적용하도록 설계했습니다` | 1 |
| ⑪ | 1090 | `개선 후만 측정한 이유는 폴링 구현을 제거해` / `<b>12s는 산술 추정</b>` | 1 / 1 |

각 `<li>` 는 선행 공백 2칸(`  <li>`)으로 시작합니다. 위 코드 블록의 원문을 그대로 복사해 치환하면 됩니다.

---

## 4. 규칙 준수 확인

- 모든 대체문 "~습니다" 체 유지, 한글 본문 + 영문 코드 식별자
- 금지 표현("해야 합니다 / 필요합니다") 미사용, 미구현 항목에 "도입했습니다" 미사용
- **수치 전부 보존**: 15s / 300ms / 9.5 → 55 req/s / 5.8배 / 20s → 2s / 0.7 → 0.2 / Top-K 5 / 12s / 1.1s / 91% / 10s / 2s
- **코드 식별자 전부 보존**: `PROCESSING`, `RagService`, `ObjectProvider`, `app.chat.history-window`(미접촉), BM25, Reranker
- 삭제로 얄팍해지지 않도록, 제거한 자백 절마다 **동등 분량의 설계 근거를 대신 배치**했습니다(④⑥⑦이 특히 그렇습니다)
