services:
  # PostgreSQL Database with pgvector extension
  db:
    image: pgvector/pgvector:pg15
    container_name: db-test
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=dev
      - POSTGRES_PASSWORD=dev123
      - TZ=Asia/Seoul
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_test:/var/lib/postgresql/data
    networks:
      - test-network
    command: postgres -c shared_preload_libraries=vector

  # Redis - Auth / Token
  redis-auth:
    image: redis:7-alpine
    container_name: redis-auth
    environment:
      - TZ=Asia/Seoul
    ports:
      - "6379:6379"
    volumes:
      - redis_auth_data:/data
    networks:
      - test-network
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # Redis Cluster - Chat (6 Nodes: 3 Master + 3 Replica)
  # ============================================

  redis-chat-1:
    image: redis:7-alpine
    container_name: redis-chat-1
    command: redis-server /etc/redis/redis.conf --port 6380 --cluster-announce-hostname host.docker.internal --cluster-announce-port 6380 --cluster-announce-bus-port 16380
    environment:
      - TZ=Asia/Seoul
    ports:
      - "6380:6380"
      - "16380:16380"
    volumes:
      - ./redis-cluster/redis-node.conf:/etc/redis/redis.conf
      - redis_chat_1_data:/data
    networks:
      - test-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: [ "CMD", "redis-cli", "-p", "6380", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 10

  redis-chat-2:
    image: redis:7-alpine
    container_name: redis-chat-2
    command: redis-server /etc/redis/redis.conf --port 6381 --cluster-announce-hostname host.docker.internal --cluster-announce-port 6381 --cluster-announce-bus-port 16381
    environment:
      - TZ=Asia/Seoul
    ports:
      - "6381:6381"
      - "16381:16381"
    volumes:
      - ./redis-cluster/redis-node.conf:/etc/redis/redis.conf
      - redis_chat_2_data:/data
    networks:
      - test-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: [ "CMD", "redis-cli", "-p", "6381", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 10

  redis-chat-3:
    image: redis:7-alpine
    container_name: redis-chat-3
    command: redis-server /etc/redis/redis.conf --port 6382 --cluster-announce-hostname host.docker.internal --cluster-announce-port 6382 --cluster-announce-bus-port 16382
    environment:
      - TZ=Asia/Seoul
    ports:
      - "6382:6382"
      - "16382:16382"
    volumes:
      - ./redis-cluster/redis-node.conf:/etc/redis/redis.conf
      - redis_chat_3_data:/data
    networks:
      - test-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: [ "CMD", "redis-cli", "-p", "6382", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 10

  redis-chat-4:
    image: redis:7-alpine
    container_name: redis-chat-4
    command: redis-server /etc/redis/redis.conf --port 6383 --cluster-announce-hostname host.docker.internal --cluster-announce-port 6383 --cluster-announce-bus-port 16383
    environment:
      - TZ=Asia/Seoul
    ports:
      - "6383:6383"
      - "16383:16383"
    volumes:
      - ./redis-cluster/redis-node.conf:/etc/redis/redis.conf
      - redis_chat_4_data:/data
    networks:
      - test-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: [ "CMD", "redis-cli", "-p", "6383", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 10

  redis-chat-5:
    image: redis:7-alpine
    container_name: redis-chat-5
    command: redis-server /etc/redis/redis.conf --port 6384 --cluster-announce-hostname host.docker.internal --cluster-announce-port 6384 --cluster-announce-bus-port 16384
    environment:
      - TZ=Asia/Seoul
    ports:
      - "6384:6384"
      - "16384:16384"
    volumes:
      - ./redis-cluster/redis-node.conf:/etc/redis/redis.conf
      - redis_chat_5_data:/data
    networks:
      - test-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: [ "CMD", "redis-cli", "-p", "6384", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 10

  redis-chat-6:
    image: redis:7-alpine
    container_name: redis-chat-6
    command: redis-server /etc/redis/redis.conf --port 6385 --cluster-announce-hostname host.docker.internal --cluster-announce-port 6385 --cluster-announce-bus-port 16385
    environment:
      - TZ=Asia/Seoul
    ports:
      - "6385:6385"
      - "16385:16385"
    volumes:
      - ./redis-cluster/redis-node.conf:/etc/redis/redis.conf
      - redis_chat_6_data:/data
    networks:
      - test-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: [ "CMD", "redis-cli", "-p", "6385", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 10

  # Redis Cluster Initialization
  redis-cluster-init:
    image: redis:7-alpine
    container_name: redis-cluster-init
    depends_on:
      redis-chat-1:
        condition: service_healthy
      redis-chat-2:
        condition: service_healthy
      redis-chat-3:
        condition: service_healthy
      redis-chat-4:
        condition: service_healthy
      redis-chat-5:
        condition: service_healthy
      redis-chat-6:
        condition: service_healthy
    volumes:
      - ./redis-cluster/create-cluster.sh:/scripts/create-cluster.sh
    entrypoint: [ "/bin/sh", "/scripts/create-cluster.sh" ]
    networks:
      - test-network
    restart: "no"

  # RabbitMQ Message Broker with STOMP plugin
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: rabbitmq-test
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
      - TZ=Asia/Seoul
    ports:
      - "5672:5672"   # AMQP protocol
      - "15672:15672" # Management UI
      - "61613:61613" # STOMP port
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - test-network
    healthcheck:
      test: [ "CMD", "rabbitmq-diagnostics", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    command: >
      sh -c "
        rabbitmq-plugins enable rabbitmq_stomp rabbitmq_web_stomp &&
        rabbitmq-server
      "

  # Frontend (Development with Hot Reload)
  frontend:
    image: node:22-alpine
    container_name: frontend-test
    working_dir: /app
    env_file:
      - ./frontend/.env.development
    environment:
      - NODE_ENV=development
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "3000:3000"
    command: sh -c "corepack enable && corepack prepare pnpm@latest --activate && pnpm install && pnpm dev"
    networks:
      - test-network
    restart: unless-stopped

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: nginx-test
    environment:
      - TZ=Asia/Seoul
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.test.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
    networks:
      - test-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  # ============================================
  # k6 부하 테스트 (Docker Compose profile: k6)
  # ============================================
  # 사용법:
  #   기본 테스트 (03_thread_exhaustion):
  #     docker compose -f docker-compose.test.yml --profile k6 run --rm k6
  #
  #   특정 스크립트 실행:
  #     docker compose -f docker-compose.test.yml --profile k6 run --rm k6 run /scripts/01_auth_check.js
  #
  #   환경변수 오버라이드:
  #     docker compose -f docker-compose.test.yml --profile k6 run --rm \
  #       -e RPS=10 -e HOLD_SECONDS=5 k6 run /scripts/04_rps_limit.js
  k6:
    image: grafana/k6:latest
    container_name: k6-load-test
    volumes:
      - ./backend/k6:/scripts
    environment:
      # 백엔드가 호스트 머신에서 실행되므로 host.docker.internal 사용
      - BASE_URL=http://host.docker.internal:8080/api
      - TEST_EMAIL=test@test.com
      - TEST_PASSWORD=password123
      - HOLD_SECONDS=8   # 스레드 점유 시간(초) — LLM 처리 시간 모의
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - test-network
    profiles:
      - k6
    # 기본 명령: 스레드 풀 고갈 테스트. docker compose run 시 오버라이드 가능.
    command: run --summary-trend-stats "avg,min,med,max,p(90),p(95),p(99)" /scripts/03_thread_exhaustion.js

networks:
  test-network:
    driver: bridge

volumes:
  postgres_data_test:
  redis_auth_data:
  redis_chat_1_data:
  redis_chat_2_data:
  redis_chat_3_data:
  redis_chat_4_data:
  redis_chat_5_data:
  redis_chat_6_data:
  rabbitmq_data:
