# 환경 변수 파일 (절대 커밋 금지!)
.env
.env.local
.env.*.local

# 각 서비스별 환경 파일
backend/.env
backend/.env.*
frontend/.env
frontend/.env.*

# Docker volumes
postgres_data/
redis_data/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# util
.claude
.idea

# tokensave 코드 인덱스 DB (로컬 전용, 10MB+)
.tokensave/

# Node / pnpm
frontend/.pnpm-store/
frontend/node_modules/
dist/
# Backend 빌드 결과
backend/build/
docs/

*.md

# ── 개인 작업 산출물은 로컬 전용 ───────────────────────────────
# 포트폴리오·검토 보고서·측정 로그는 저장소에 넣지 않는다
# docs/ 와 *.md 가 대부분을 덮지만 report 는 확장자가 늘 수 있어 경로로 못박는다
backend/report/
